package routeURL

const (
	Base       = "/api"
	Login      = Base + "/login"
	Register   = Base + "/register"
	GetTodos   = Base + "/getTodos"
	AddTodo    = Base + "/addTodo"
	GetTodo    = Base + "/getTodo"
	UpdateTodo = Base + "/updateTodo"
	DeleteTodo = Base + "/deleteTodo"
)
